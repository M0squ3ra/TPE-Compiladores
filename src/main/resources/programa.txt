Mi_Progama;

INT a;

INT FUNC myFunc2 (INT v)
BEGIN
    RETURN(2);
END;

INT FUNC myFunc (INT var) 
BEGIN
    var := var + myFunc2(2);
    RETURN(var);
END;

BEGIN
    //IF (a < 1) THEN
        a := a + 1;
    ELSE
        a := a - 1;
    ENDIF;//
    //a := myFunc(SINGLE(a));//

    PRINT(%Hola mundo%);
    a := myFunc(2);
    PRINT(%Hola mundo2%);

END $