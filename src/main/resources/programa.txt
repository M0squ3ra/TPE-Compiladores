Mi_Progama;

INT a,b;
SINGLE c;

INT FUNC myFunc (SINGLE a) 
INT d; 
BEGIN
    d := 2;
    RETURN(d + 1);
END;

BEGIN
    //a := 1;//
    //IF (a < 1) THEN
        a := a + 1;
    ELSE
        a := a - 1;
    ENDIF;//
    //a := myFunc(SINGLE(a));//

    //REPEAT (a := 1; a < 5; 5)
    BEGIN
        a := a + 1;
    END;//
    PRINT(%Hola mundo%);
    PRINT(%mundo%);
    PRINT(%Chau mundo!!!%);
    a := a + 1 - 2;

END $