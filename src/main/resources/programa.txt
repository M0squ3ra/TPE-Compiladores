Mi_programa_hello;

//SINGLE a_2, _abc, b_1;//

//FLOAT FUNC errores_lexicos (INT props)//

INT a, b, c, d, e, f;
SINGLE z;

BEGIN
    //a := 32768;//
    PRINT(%Fuera del loop%);
    REPEAT(a := 0; a < 4 + 1 * 20 - 1; 1)
    BEGIN
        PRINT(%Dentro del loop%);
    END

    //a := 1;
    a := 1 + 2;
    z := SINGLE(2);
    z := SINGLE(1) + SINGLE(2);
    z := SINGLE(1) + 2.0;
    z := SINGLE(1 + 2) + 2.0;
    z := SINGLE(1 + 2) + SINGLE(3 + 4 - 1) - 2.0;//


END