Mi_Progama;

INT a,b;
SINGLE c;

SINGLE FUNC myFunc (SINGLE a) 
INT d; //si pongo c no me genera otro ambito para la variable //
SINGLE e; 
BEGIN
    d := 2;
    RETURN(SINGLE(d) + b);
END;

BEGIN
    a := a - SINGLE(b) + 1; // si declaro una funcion, y retorna algo de distinto tipo, tengo el problema de que aca va a error cuando se haga el chequeo de tipos porque no va a estar el terceto//
    c := SINGLE(1);
    c := SINGLE(a + 1) + 2.1;
    c := SINGLE(a + 1);


END $