Mi_Progama;

INT a;

INT FUNC myFunc (INT var) 
BEGIN
    var := var + 2;
    RETURN(var);
END;

BEGIN
    //IF (a < 1) THEN
        a := a + 1;
    ELSE
        a := a - 1;
    ENDIF;//
    //a := myFunc(SINGLE(a));//

    PRINT(%Hola mundo%);
    a := myFunc(2);
    PRINT(%Hola mundo2%);

END $